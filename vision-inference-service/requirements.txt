# H15 fix: every dependency below is pinned so identical container builds
# produce identical inference behaviour. A floating `ultralytics>=8.3.0`
# upgrade has previously changed the YOLO11n head, silently shifting all
# confidence scores by ~2–3% and breaking the PPE thresholds tuned in
# config.py. Re-pin (and re-bench) deliberately when bumping versions.
#
# The CLIP package is pinned to an explicit commit because the
# `ultralytics/CLIP` master branch is mutable. Verified commit reproduces
# the OpenCLIP weights used by the open-vocab grounding detector.
fastapi==0.115.6
uvicorn==0.32.1
transformers==4.46.3
ultralytics==8.3.40
roboflow==1.1.50
Pillow==10.4.0
python-multipart==0.0.20
boto3==1.35.81
timm==1.0.12
opencv-python-headless==4.10.0.84
httpx==0.27.2
python-dotenv==1.0.1
cloudinary==1.41.0
inference_sdk==0.31.0
numpy==1.26.4
scipy==1.14.1
pyyaml==6.0.2
requests==2.32.3
shapely==2.0.6
prometheus-client==0.21.1

# Face recognition (employee identification on violation frames). dlib is the
# native backend of face-recognition and compiles from source — the Dockerfile
# already installs the required cmake/build-essential toolchain. face-recognition
# is pinned exactly; setuptools is capped because face-recognition-models imports
# pkg_resources, which was removed from newer setuptools releases; dlib uses a
# floor pin because prebuilt wheel availability varies by platform/Python version.
setuptools<81
face-recognition==1.3.0
dlib>=19.24
