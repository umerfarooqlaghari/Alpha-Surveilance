# Installed with --no-deps — see the explanation in requirements.txt.
# Keeping it in its own file means the version is declared once and both the
# Dockerfile and run.sh stay in step.
face-recognition==1.3.0
